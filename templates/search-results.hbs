{{~ #*inline "page"}}
<div class="container search-results">
    <h1>
        <img src="/static/img/bible.rs.svg" alt="BIBLE.RS" height="100" width="100" class="logo">
        {{@root.title}}
    </h1>
    <nav>
        <ol>
            {{~ #each matches as |match|}}
            <li>
                <a href="{{match.link.url}}"><strong>{{match.link.label}}</strong> | {{{match.text}}}</a>
            </li>
            {{~ /each}}
        </ol>
    </nav>
</div>
{{~ /inline}}
{{~> base ~}}
